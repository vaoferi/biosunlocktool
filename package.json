{
  "name": "biosunlocktool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "wrangler pages deploy . --project-name biosunlocktool"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
